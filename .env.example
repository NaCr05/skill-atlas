# Optional. Local inventory and deterministic Prompts work without these values.
SKILLSMP_API_KEY=
VERCEL_OIDC_TOKEN=
GITHUB_TOKEN=
# Optional AI Prompt enhancement. Keep both providers configured if desired;
# AI_PROVIDER selects one without automatic request-time failover.
AI_PROVIDER=auto
OPENAI_API_KEY=
OPENAI_MODEL=
DEEPSEEK_API_KEY=
# Example: deepseek-v4-flash
DEEPSEEK_MODEL=
